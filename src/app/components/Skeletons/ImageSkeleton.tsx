import styles from "./ImageSkeleton.module.css"

/**
 * @description A CSS animated Image components that serves as a visual loading state when loading images
 * @returns {JSX.Element}
 */
export default function ImageSkeleton() {
    return <div className={styles.layout} />
}